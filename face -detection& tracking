import cv2#opencv-python
haar_cascade = cv2.CascadeClassifier("C:/Users/HCT/OneDrive/Pictures/Screenshot 2025-10-21 195357.png") #loading into name file
cam=cv2.VideoCapture(1) #Initializing cameraid
while True: #Infinite loop
    _,img=cam.read() #Reading Frame from camera
    grayImg= cv2.cvtcolor(img, cv2.COLOR_BGR2GRAy)#converting color to gray color
    faces=haar_cascade.detectMultiScale(graying,1.3,4) #Getting coordinates
    for (x,y,w,h) in faces:
        cv2.rectangle(img,(x,y),(x+w,Y+h),(0,255,0),9) #drawing rectangle
    cv2.imshow("FaceDetection",img) #Display the frame
    key=cv2.waitKey(10)
    print(key)
    if key==27: #escape key tyo exit
        break
cam.release()
cv2.destroyAllWindows()
